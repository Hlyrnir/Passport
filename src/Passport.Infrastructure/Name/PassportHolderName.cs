namespace Passport.Infrastructure.Name
{
    internal enum PassportHolderColumn
    {
        ConcurrencyStamp,
        CreatedAt,
        CultureName,
        EditedAt,
        EmailAddress,
        EmailAddressIsConfirmed,
        FirstName,
        Id,
        LastName,
        PhoneNumber,
        PhoneNumberIsConfirmed,
        SecurityStamp
    }

    internal enum PassportHolderTable
    {
        PassportHolder
    }
}